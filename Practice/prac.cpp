#include<iostream>
#include<vector>
#include<cmath>
using namespace std;

int main(){

    int D, G;
    vector<int> p, c;

    cin >> D >> G;
    p.resize(D);
    c.resize(D);
    for (int i = 0; i < D; i++){
        cin >> p[i] >> c[i];
    }
}